{
  "name": "rolo-dev",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "rolo.dev — the cosy little desktop companion. Marketing site.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name=rolo-dev --commit-dirty=true",
    "deploy:preview": "astro build && wrangler pages deploy ./dist --project-name=rolo-dev --branch=preview --commit-dirty=true",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami"
  },
  "dependencies": {
    "astro": "^6.3.4"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
